# 複製此檔為 .env 並填入你的設定
# cp .env.example .env

# Proxy 設定（沒有 Proxy 可以留空）
HTTP_PROXY=
HTTPS_PROXY=

# 模型儲存根目錄（預設為執行目錄下的 models/）
SAVE_BASE=/data/model_hf

# HuggingFace Token（需要授權的模型才需要）
# 到 https://huggingface.co/settings/tokens 取得
HF_TOKEN=
